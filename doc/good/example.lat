int foo(int a) {
    a++;
    printInt(a);
    a--;
    return 2 * a + 1;
}

void main() {
    int a;
    int b = 1, c;
    int d = 2;
    string nope = "asd";
    printString(nope);

    printInt(b);
    printInt(a);

    if (d == 2) {
        b = 3;
        printInt(b);
    } else {
        if (a == 0) {
            while (c) {
                c--;
            }
        }
    }

    {
        int temp;
    }

    d = foo(b);

    printInt(d);

	return;
}
